# Frank Bot Configuration (minimal)
#
# Policy: no secrets in `.env` except Vault AppRole connection info.
# All service secrets live in Concordia Vault under `secret/frank-bot/*`
# and are managed by this repo’s Terraform at `terraform/vault/`.
#
# See:
# - `terraform/vault/` (service-owned Vault config)
# - `scripts/vault_audit.py` (checks what’s missing)

# Vault connection (AppRole)
VAULT_ADDR=http://concordia-vault:8200
VAULT_ROLE_ID=your-role-id
VAULT_SECRET_ID=your-secret-id

# -----------------------------------------------------------------------------
# Optional (non-secret) config
# -----------------------------------------------------------------------------

# Public origin used in manifests & OpenAPI servers[]
# PUBLIC_BASE_URL=https://frank-bot-api.contrived.com

# Android phone ADB targeting
# Preferred: USB ADB serial (example: 48151FDKD001UD)
# ANDROID_DEVICE_SERIAL=48151FDKD001UD
#
# Optional fallback: wireless debugging host/port
# ANDROID_ADB_HOST=10.0.0.95
# ANDROID_ADB_PORT=5555
