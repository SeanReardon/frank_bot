# Frank Bot Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# VAULT CONFIGURATION (Production)
# =============================================================================
# In production, secrets are loaded from HashiCorp Vault.
# Only these three variables are needed - all secrets come from Vault.

VAULT_ADDR=http://concordia-vault:8200
VAULT_ROLE_ID=your-role-id
VAULT_SECRET_ID=your-secret-id

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

PORT=8000
HOST=0.0.0.0
PUBLIC_BASE_URL=https://frank-bot-api.contrived.com
LOG_PATH=/home/frank_bot/logs

# OpenAI Actions API key (for ChatGPT integration)
ACTIONS_API_KEY=your-api-key

# Google OAuth token file (state, not a secret)
GOOGLE_TOKEN_FILE=token.json
GOOGLE_REDIRECT_URI=http://localhost:8080/

# =============================================================================
# DEVELOPMENT ONLY - Environment Variable Fallback
# =============================================================================
# If Vault is not configured, these environment variables are used as fallback.
# In production, leave these unset and use Vault instead.

# Google OAuth (from Google Cloud Console)
# GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
# GOOGLE_CLIENT_SECRET=GOCSPX-your-secret

# Stytch authentication
# STYTCH_PROJECT_ID=project-live-xxx
# STYTCH_SECRET=secret-live-xxx

# Telegram API (from my.telegram.org)
# TELEGRAM_API_ID=12345678
# TELEGRAM_API_HASH=your-api-hash
# TELEGRAM_PHONE=+12125551234

# Telnyx SMS
# TELNYX_LET_FOOD_INTO_CIVIC_KEY=KEY...
# TELNYX_PHONE_NUMBER=+12125551234

# Swarm/Foursquare
# SWARM_OAUTH_TOKEN=your-oauth-token
# FOURSQUARE_API_KEY=fsq3...

# =============================================================================
# JORBS AUTONOMOUS TASK SYSTEM
# =============================================================================
# Jorbs are long-lived autonomous tasks that can send messages on your behalf.

# OpenAI API key for LLM-powered task execution (uses gpt-5.2 model)
# OPENAI_API_KEY=sk-...

# Jorb storage and logging
JORBS_DB_PATH=./data/jorbs.db
JORBS_PROGRESS_LOG=./data/jorbs_progress.txt

# Agent policy settings
AGENT_SPEND_LIMIT=100.0          # Max spending (USD) before requiring approval
CONTEXT_RESET_DAYS=3             # Days before context is reset (Ralph Loop)

# Message debouncing (seconds to wait for additional messages before processing)
DEBOUNCE_TELEGRAM_SECONDS=60     # Telegram messages within 60s are bundled
DEBOUNCE_SMS_SECONDS=30          # SMS messages within 30s are bundled

# Email/SMTP for notifications and daily digest
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=app-specific-password
# DIGEST_EMAIL_TO=your-email@gmail.com
# DIGEST_TIME=08:00              # Time to send daily digest (24h format)
